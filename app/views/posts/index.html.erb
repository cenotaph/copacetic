<h1>The Copacetic Blog</h1>

<span class="front_article_body">Here is a blog where we will post our latest thoughts, news or other comments.</span>

<Br /><br/>

<% for post in @posts %>
<div class="blogpost">
  <p class="blogtitle"><%= link_to post.title, post %></p>
  <SPAN class="blogmeta">Posted by <%= h post.user.login %> on <%= h post.created_at.strftime("%d %B %H:%m") %> (<%= time_ago_in_words post.created_at %> ago)</span>
	<br />
	
    <div class="blogbody"><%= post.body %></div>
<% if is_admin? %> 
   <%= link_to 'Edit this post', edit_post_path(post) %><br /><% end %>
  <% if post.approved_comments.size == 0 %>
  No comments.
    <%= link_to('Leave the first comment!', '/posts/' + post.id.to_s + '#comments' ) %>
    <% else %>
      <%= link_to(pluralize(post.approved_comments.size, 'comment'), '/posts/' + post.id.to_s + '#comments') %>...
    <% end %>
 <br clear="all">
</div>
   <hr width="40%">
   
 <% end %>


<br />
<% if is_admin? %> 
  <%= link_to 'New post', new_post_path %>
<% end %>